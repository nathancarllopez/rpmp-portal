// To do: Finish this

// import { supabase } from "./client";
// import { TableName } from "./types";

// export default async function deleteTableRows(tableName: TableName, idsToDelete: number[]) {
//   const response = await supabase.from(tableName).delete().in('id', idsToDelete);
// }